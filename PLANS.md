# 研政 · Politics Lab 独立项目 ExecPlan

## 目标

另建一个与原 `mti-shangan` 完全分离的考研政治学习网站。它根据 Chrome 实测的私有夸克目录组织课程路径，围绕“看课 → 做题 → 闭卷输出”形成每天可执行、可验收、可跨设备同步的闭环，并独立部署到 GitHub Pages。

## 已核对资料

- 徐涛强化主课：马原 19、思修 8、史纲 10、毛中特 7、新思想 12，共 5 科 56 课次（含 3 个阶段测试）。
- 27 版肖1000：试题分册与答案解析册均存在。
- 逐题视频：目前只核对到马原与史纲目录，不能标记为五科全覆盖。
- 当前缺口：年度时政、冲刺课程、完整历年政治真题与逐科错题视频。
- 今日起点：马原导论，视频 53:04；肖1000 导论范围为单选 1—7、多选 1—2。

## 边界

- 课程视频、讲义、肖1000题面与解析继续留在用户私有夸克或实体书中；网站只保存相对路径、原创自测、进度与训练证据。
- 原创题必须清楚标注“非肖1000、非真题”。真题模块只保存年份、来源、得分与复盘，不公开传播受版权保护题面。
- 前端使用 React + TypeScript + Vite；数据库采用现有 Supabase 项目中的独立 `politics_*` 表与 Supabase Auth，不复用旧站共享数据表。
- 未登录时本机可完整使用；登录后才向 `auth.uid()` 对应记录同步。浏览器端不出现 service-role key。

## 里程碑

- [x] 用 Chrome 核对夸克主线、课次数、首日课程与资料缺口。
- [x] 生成独立桌面与移动端视觉基准。
- [x] 完成课程、练题、错题、真题记录、资料审计与数据趋势页面。
- [x] 完成 Supabase 邮箱登录、分账号本机缓存、冲突合并与 RLS，并在线应用、验证最小权限。
- [x] 完成单元测试、Playwright 与 Chrome 桌面/移动验收。
- [x] 创建独立 GitHub 仓库、部署 Pages 并用 Chrome 核对线上页面。

## 验收条件

1. 首屏只有一条明确路线：马原导论 53:04 → 肖1000 导论 9 题 → 闭卷框架与错因复盘。
2. 五科 56 课次可浏览、标记进度，课程只通过用户私有夸克入口打开。
3. 原创自测支持作答、解析、错题留存与重练；外部肖1000练习可记录题量、正确率、错因和框架。
4. 资料审计准确显示已覆盖、部分覆盖和缺口，不把当前资料说成百分之百完整。
5. 未登录本机可用；登录后 Supabase RLS 确保用户只能访问自己的状态与事件。
6. lint、typecheck、unit、build、desktop/mobile e2e 通过；GitHub Pages 线上可访问。

## 2026-07-29 Apple 风格 UI 重设计

### 视觉方向

- [x] 以 Apple HIG 的层级、和谐、一致性为原则，生成桌面与手机完整视觉基准。
- [x] 将深蓝/红色报刊风改为实白与 `#f5f5f7` 内容层、浅色半透明导航层和 Apple 蓝主操作。
- [x] 把首页三张割裂卡片重构为一个连续的“当前课程 + 后续两步”执行界面。
- [x] 统一课程、练题、错题、真题、资料与趋势页面的排版、表单、列表、状态和响应式系统。
- [x] 补强稳定语义选择器，并完成桌面/手机浏览器视觉与交互验收。
- [ ] 更新 GitHub Pages，核对线上版本与本地设计一致。

### Apple 风格验收条件

1. 导航层可使用轻量半透明材质，内容层保持实色，不在所有卡片上滥用玻璃效果。
2. 首页第一视口保留“今天只做三件事”，三步名称和主操作清晰，桌面与 390px 手机均无裁切或横向滚动。
3. 字体只使用系统无衬线栈，中文正文不小于 13px，主要触控目标不小于 44px。
4. 主操作统一使用 `#0071e3`，红绿橙只承担错误、完成、提醒等语义状态。
5. 课程 56 项、题源边界、资料缺口、状态持久化与 Supabase 同步功能不因视觉重构退化。
6. 最新视觉稿与浏览器截图在同一轮 `view_image` 检查中达到可交付一致性。

## 2026-07-29 Laper 工作台布局重构

### 目标与视觉依据

- [x] 在已登录 Chrome 中实测 `https://laper.ai/app/new`，保存 1440×1024 桌面、收起侧栏、快捷面板、390×844 手机和手机菜单状态。
- [x] 明确只借鉴布局骨架、留白、信息密度与响应式交互，不复制 Laper 商标、账号、文案或素材。
- [x] 生成研政桌面与手机视觉基准：`design/laper-inspired-desktop-concept.png`、`design/laper-inspired-mobile-concept.png`。
- [x] 将首页改为 240px 轻侧栏 + 单一居中学习任务 + 快捷动作 + 今日通关上下文；其余页面沿用同一壳层。
- [x] 桌面支持侧栏收起，手机使用顶部菜单与全屏抽屉，不保留底部标签栏。
- [x] 修复 Hash 路由与页内焦点冲突，把课程/夸克打开动作改为真实链接，并逐一验证七个站内入口。

### 数据库边界

- [x] 保留 Supabase Auth、`politics_user_state`、`politics_profile`、`politics_study_events`、RLS、乐观并发同步与本机离线降级。
- [x] 新壳层必须明确显示“本机模式 / 正在同步 / 已同步 / 同步异常”，并可打开登录与数据同步设置。
- [x] 校验迁移约束、索引、RLS 与最小权限；本地无密钥时不得伪装为已连接数据库。

### 验收条件

1. 1440×1024 首屏在结构、留白、侧栏宽度、主任务位置、轻边框和操作密度上与选定视觉基准一致。
2. 390×844 无横向滚动；汉堡菜单可打开、关闭并完成七个入口的路由跳转。
3. `今日执行 → 课程 / 练题 / 错题 / 真题 / 资料` 快捷入口都到达正确页面，页内“跳到主要内容”不再破坏 Hash 路由。
4. “开始学习”和课程列表使用可访问的真实外链；打开私有夸克的同时保留当前课程与学习状态。
5. Supabase 未配置时完整本机可用；配置并登录后按 `auth.uid()` 同步，任何用户不能读写其他用户数据。
6. lint、typecheck、unit、build、桌面/手机 e2e 与 Browser 控制台、截图、交互验收全部通过。

## 2026-07-29 政治抽背模块

### 目标与范围

- [x] 参考原 `mti-shangan` 的抽背流程，新增独立的“政治抽背”导航与首页快捷入口。
- [x] 依据夸克主线顺序（马原 → 思修 → 史纲 → 毛中特 → 新思想）整理 60 张原创核心知识点卡，不复制肖1000或历年真题题面。
- [x] 支持“显示答案 → 再看 / 模糊 / 会了”判定、错题回流、到期复习、五科筛选和键盘操作。
- [x] 抽背进度并入现有本机状态与 Supabase JSON 状态同步，兼容旧版状态快照，不新增公开课程内容或密钥。
- [x] 完成调度单元测试、桌面/手机端到端交互和设计 QA。

### 验收条件

1. `/recall` 首屏直接进入单题闭卷抽背，答案默认不可见；空格翻面，1/2/3 完成复习判定。
2. “再看”卡进入错题筛选且本轮稍后重现；“模糊”次日到期；“会了”按间隔递增排期。
3. 60 张卡覆盖五科，顺序与夸克主课一致，每张标明“原创核心抽背（非肖1000、非历年真题）”。
4. 旧的 schema v1 本机或云端状态可继续载入，抽背记录在多设备合并时按最后复习时间保留较新版本。
5. 1440×1024 为双栏专注布局，390×844 为卡片优先的单列布局，无横向滚动、无相关控制台错误。
6. lint、typecheck、unit、build、desktop/mobile e2e 全部通过。

## 2026-07-29 标准答案与间隔复盘

### 目标与依据

- [x] 60 张卡逐张挂接高等教育出版社 2023 年版“马工程”对应教材；新思想同时标注中央最新公开学习材料，页面展示核对版本、日期和官方链接。
- [x] 把“可背诵答案”升级为“标准化参考答案 + 采分点”，并明确最终以当年考试大纲、命题和阅卷要求为准，避免把自编表述包装成唯一标准答案。
- [x] 每次“再看 / 模糊 / 会了”都写入不可覆盖的复习历史，本机持久化并随现有 Supabase JSON 状态同步，多设备按事件 ID 合并。
- [x] 用“10 分钟 → 1 天 → 2 天 → 4 天 → 7 天 → 15 天 → 30 天 → 60 天”的艾宾浩斯启发式间隔；错答回到 10 分钟，模糊次日复习，会了逐级推进。
- [x] 增加今日复盘、最近 7 天记录、掌握率和下一次复习时间，刷新页面后记录仍在。
- [x] 完成数据约束、旧状态兼容、调度单元测试、桌面/手机 E2E 与真实浏览器验证。

### 验收条件

1. 每张卡都能看到官方教材依据、版本和核对日期；数据测试阻止缺少依据、采分点或有效官方链接的卡进入构建。
2. 每次判定生成一条唯一历史事件，包含卡片、结果、复习时间、前后阶段和下次到期时间；旧 schema v1 状态无历史字段时仍能载入。
3. “再看”约 10 分钟后到期，“模糊”次日到期，“会了”按 1/2/4/7/15/30/60 天推进；界面说明这是启发式默认值而非唯一遗忘曲线公式。
4. 今日复盘实时展示抽背次数、不同判定、掌握率和最近事件；最近 7 天按天展示并在刷新后保留。
5. lint、typecheck、unit、build、desktop/mobile e2e 与 Browser 控制台、截图、刷新持久化全部通过。

## 2026-07-29 沉浸式全屏抽背

### 目标与范围

- [x] 把 `/recall` 拆成“今日抽背准备 → 全屏专注抽背 → 退出查看复盘”三段式流程。
- [x] 全屏模式覆盖应用壳层，放大问题、完整参考答案、采分关键词和判定操作，并支持 Esc 退出。
- [x] 为每类题补充三步完整作答结构；保留教材版本、核对日期和官方书目链接，不宣称唯一标准答案。
- [x] 保留现有 60 张卡、五科筛选、键盘判定、不可覆盖历史、间隔排程和 Supabase 状态同步。
- [x] 完成桌面/手机端交互、无裁切、无横向滚动、控制台与刷新持久化验证。

### 验收条件

1. 进入 `/recall` 默认只显示准备面板和今日复盘，点击“开始全屏抽背”后才出现单题工作区。
2. 全屏工作区覆盖侧栏与普通页面，桌面答案区独立滚动；手机整页纵向滚动，完整答案、采分点和教材依据均可到达。
3. 空格显示完整答案，1/2/3 完成判定，Esc 或“退出抽背”回到准备页，已完成记录即时保留。
4. 每张已揭示卡显示“完整标准化参考答案”和对应题型的三步作答结构，同时保留当年考试口径提示。
5. lint、typecheck、unit、build、desktop/mobile e2e 与 Browser 截图、控制台、交互验收全部通过。
